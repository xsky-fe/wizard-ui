import { Canvas, Meta, ArgTypes } from '@storybook/blocks';

import * as InputDropdownStories from './InputDropdown.stories.tsx';

<Meta of={InputDropdownStories} />

# InputDropdown
作为 Input 一部分的下拉框

## 使用方式
通常和 Input 一起使用，在 Input 的前面或者后部，用来增强 Input 功能

## 基本用法

<Canvas of={InputDropdownStories.Basic}/>

## API
<ArgTypes />