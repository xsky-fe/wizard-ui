{/* Button.mdx */}

import { Canvas, Meta,ArgTypes } from '@storybook/blocks';

import * as ButtonStories from './Button.stories.tsx';

<Meta of={ButtonStories} />

# Button

按钮用于开始一个即时操作，触发业务逻辑时使用

## 使用场景

标记了一个（或封装一组）操作命令，响应用户点击行为，触发相应的业务逻辑。

## 基本用法

### 按钮状态

根据操作类型的场景可以选择七种格式：

<Canvas of={ButtonStories.Variant} />

### 按钮大小

按钮可以设置成四种大小：large（lg）、默认、small（sm）、xsmall（xs）

<Canvas of={ButtonStories.Size} />

### 按钮占据整行

<Canvas of={ButtonStories.Block} />

### 按钮文字提示

<Canvas of={ButtonStories.Tooltip} />

### 激活&禁用按钮
<Canvas of={ButtonStories.Active} />




## API
<ArgTypes />
