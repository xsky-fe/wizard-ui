language: node_js
node_js:
  - stable
branches:
  only:
    - master
cache:
  yarn: true

jobs:
  include:
    - stage: "Tests"
      name: "Unit Tests"
      script: yarn test

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GH_TOKEN  # Set in the settings page of your repository, as a secure variable
  keep_history: true
  on:
    branch: master.
